module "checkout-shop__payment"

match / checkout-shop__payment {
    <select>
        apply .payment.list checkout-shop__payment-item
    </select>
}

match .list checkout-shop__payment-item {
    <option value="{ .code }">
        if (.code == ..selected) {
            @selected="selected"
        }
        .title
    </option>
}